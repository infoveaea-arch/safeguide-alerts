# PDF text cache — persisted via GitHub Actions cache, never committed
alerts/.state/
# never commit keys/secrets (CI deploy key lives in GitHub Actions secrets only)
*_ed25519
*_ed25519.pub
*.pem
id_rsa*
__pycache__/
*.pyc
